# requirements.txt — app22 (Streamlit Cloud + Google Sheets via gspread)
# Recommandé : Python 3.12 (ou 3.13 si toutes les dépendances passent)

streamlit>=1.32
pandas>=2.2.0
numpy>=1.26.0
openpyxl>=3.1.2
gspread>=6.1.0
google-auth>=2.30.0
